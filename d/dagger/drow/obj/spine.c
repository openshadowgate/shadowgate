#include <std.h>
inherit OBJECT;

void create() {
    ::create();
    set_id(({"spine"}));
    set_short("the spine of Grimtooth");
    set_long("This is the %^RED%^bloody%^RESET%^ spine "
    	"of Grimtooth."
);
    set_weight(1);
    set_value(0);
}

