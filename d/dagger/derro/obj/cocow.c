inherit "/std/Object";
void create(){
  ::create();
  set_name("side of beef");
  set_id(({"cooked cow","cow"}));
  set_short("cooked cow");
  set_long(
  "  This is a massive cow cooked to perfection the way fire giants like em."
  "  Which means of course that there is no way you could stomach it."  
   "  Doesn't smell that bad, but how much would you trust the kitchen it came out of?"
  );
  set_weight(100);
  set_value(10);
}
