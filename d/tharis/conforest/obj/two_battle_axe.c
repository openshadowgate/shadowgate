inherit "/d/common/obj/weapon/large_battle_axe";
/* inherit "/std/weapon";

void create()
{
  ::create();
  set_id(({"axe","battle axe","two_battle_axe"}));
  set_name("battle axe");
  set_short("A two-handed battle axe");
  set_long("A very heavy, double edged axe blade "+
	"has been attached to a thick metal pole.  It "+
	"looks almost too heavy to use, but would "+
	"split apart most anything it hits.");
   set_weight(20);
   set_size(3);
   set_value(45);
   set_wc(1,10);
   set_large_wc(1,10);
   set_type("slash");
   set_prof_type("large axe");
   set_weapon_speed(10);
} */
