inherit "/std/Object";

void create() {
  ::create();
    set_name("ember");
    set_id(({"dekukinordaemberkey","ember"}));
    set_short("%^RED%^A glowing ember%^RESET%^");
    set_long("%^RED%^This is a large glowing ember that looks like "+
    "it was recently pulled from a very hot fire.  However, "+
    "some type of light %^BLUE%^blue%^RESET%^%^RED%^ barrier "+
    "surrounds it, containing its destructive heat and making "+
    "it safe to carry.%^RESET%^");
    set_value(50);
    set_weight(1);
}
