#include <std.h>
#include "../../antioch.h"
inherit "/d/common/mounts/mare";
/*inherit "/std/riding_animal.c";

void create()
{
	::create();
	set_name("gray mare");
	set_short("A gentle gray mare");
	set_id(({"horse","mare","gray mare","gentle mare","gentle gray mare"}));
	set_long(
	"This is a gentle gray mare. She is dappled with white spots on"+
	" her hind quarters, and has a white star on her forehead. Her"+
	" mane and tail are a darker gray. She seems quite sweet and docile."
	);
	set_gender("female");
	set_race("horse");
	set_body_type("equine");
	set_size(2);
	set_hd(1,6);
	set_max_hp(6);
	set_hp(query_max_hp());
	set_value(50);
	set_max_distance(500);
	set_enter_room(" trots in on a gray mare.");
	set_exit_room("trots out on a gray mare.");
	set_vehicle_short("Mare");
	set_attack_limbs(({"right forehoof","left forehoof"}));
	set_attacks_num(1);
	set_damage(1,4);
   set_riding_level(3);
}
*/