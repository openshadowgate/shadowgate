#include <std.h>
#include "../../antioch.h"
inherit "/d/common/mounts/gelding";
/*inherit "/std/riding_animal";

void create()
{
	::create();
	set_name("gelding");
	set_short("A dun colored gelding");
	set_id(({"gelding","horse","dun colored gelding","dun gelding"}));
	set_long(
	"This is a tame, dun colored gelding. He looks to be well trained"+
	" and in good shape. His mane and tail are a dark brown color and"+
	" his eyes are speckled with different shades of brown."
	);
	set_gender("male");
	set_race("horse");
	set_body_type("equine");
	set_size(2);
	set_hd(1,6);
	set_max_hp(6);
	set_hp(query_max_hp());
	set_value(75);
	set_max_distance(500);
	set_enter_room(" rides in on a tame gelding.");
	set_exit_room("rides out on a tame gelding.");
	set_vehicle_short("Gelding");
	set_attack_limbs(({"right forehoof","left forehoof"}));
	set_attacks_num(1);
	set_damage(1,4);
    set_riding_level(6);
}*/
