#include <std.h>
inherit "std/quest_ob";
void create() {
  set_name("Finished Grizon's Job");
    set_id(({"Finished Grizon's Job","grizon quest","grizon2"}));
    set_short("Finished Grizon's Job object");
   set_quest_points(10);
}