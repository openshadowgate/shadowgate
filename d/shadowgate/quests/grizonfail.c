#include <std.h>
inherit "std/quest_ob";
void create() {
  set_name("Failed Grizon's Job");
    set_id(({"Failed Grizon's Job","grizon quest","grizon1"}));
    set_short("Failed Grizon's Job object");
   set_quest_points(10);
}