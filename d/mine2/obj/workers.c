#include <std.h>
inherit OBJECT;

create()
{
  ::create();
  set_weight(5000);
  set_id(({ "workers" }));
  set_name("workers");
  set_short("Several workers");
  set_long("The workers are busy at work clearing the cave enterance so "
"that you can get in.  They are being very carfull and making sure that they "
"do not set of another rock slide."
"The workers are all carring shovels and picks and are sweeting horribly.  "
"They moan and grown as they work hard to help you.  You should probably give "
"them a hand, but you would probably only be in the way.  "
"They keep on saying almost done when they arn't.");
}












