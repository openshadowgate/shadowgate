//Changed to inherit common lrobe.c -Cythera 4/05
inherit "/d/common/obj/armour/lrobe.c";

void create() {
    ::create();
   // set_name("robe");
    //set("id", ({ "robe", "armor" }) );
    //set("short", "A basic robe");
    //set("long", "This is the simplest type of armor, fashioned from quilted layers of cloth and batting. It tends to get //hot, and after a time becomes foul with sweat, grime, lice and fleas.");
  //  set_weight(10);
    //set("value", 4);
    //set_type("clothing");
    //set_limbs( ({ "torso" }) );
    //set_ac(1);
    //set_size(3);
}
