#include <std.h>
inherit OBJECT;
create() {
::create();
set_id(({ "scroll" }));
set_name("scroll");
set_short("A Scroll");
set_long("It is a note from the local king.  As you look closer, you can see that it has been signed by the king, with a message saying that there is an invasion comming.");
}
