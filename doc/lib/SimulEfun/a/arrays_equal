int arrays_equal(mixed *array_one, mixed *array_two);

DESCRIPTION
Tests if the arrays are exactly equal to each other.
Same number of members, each member is exactly equal
to same indexed member in each array.  If arrays are
not exactly equal, will return 0

LOCATION
/adm/simul_efun/arrays_equal.c